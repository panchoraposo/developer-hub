apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: devspaces
  namespace: openshift-gitops
spec:
  project: default
  source:
    repoURL: https://github.com/panchoraposo/developer-hub.git
    targetRevision: main
    path: apps/devspaces/overlays/dev
  destination:
    server: https://kubernetes.default.svc
    namespace: dev-spaces
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true 