apiVersion: rhdh.redhat.com/v1alpha3
kind: Backstage
metadata:
  name: developer-hub
spec:
  application:
    appConfig:
      mountPath: /opt/app-root/src
      configMaps:
         - name: developer-hub-config
    
    replicas: 1
    route:
      enabled: true
  database:
    enableLocalDb: true